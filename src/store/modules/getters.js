export default{
    getIsDisplay(state) {
        return state.isDisplay
    },
    getCurrents(state) {
      return state.current
    },
    getBreadListState(state) {
        return state.breadListState
    }
}
